# --- НАЧАЛО ФАЙЛА .htaccess ---

# 1. ПЕРЕАДРЕСАЦИЯ НА HTTPS И УДАЛЕНИЕ WWW
# Это важно для SEO (склеивает зеркала) и безопасности
<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Если сайт открыт без https -> редирект на https
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

  # Если сайт открыт с www (www.site.by) -> редирект на без www (site.by)
  # Если вам нужно наоборот, удалите этот блок
  RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
  RewriteRule ^(.*)$ https://%1/$1 [R=301,L]
</IfModule>

# 2. СЖАТИЕ GZIP (Уменьшает вес HTML, CSS, JS файлов на 60-80%)
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html
  AddOutputFilterByType DEFLATE text/css
  AddOutputFilterByType DEFLATE text/javascript
  AddOutputFilterByType DEFLATE text/xml
  AddOutputFilterByType DEFLATE text/plain
  AddOutputFilterByType DEFLATE image/x-icon
  AddOutputFilterByType DEFLATE image/svg+xml
  AddOutputFilterByType DEFLATE application/rss+xml
  AddOutputFilterByType DEFLATE application/javascript
  AddOutputFilterByType DEFLATE application/x-javascript
  AddOutputFilterByType DEFLATE application/xml
  AddOutputFilterByType DEFLATE application/xhtml+xml
  AddOutputFilterByType DEFLATE application/x-font
  AddOutputFilterByType DEFLATE application/x-font-truetype
  AddOutputFilterByType DEFLATE application/x-font-ttf
  AddOutputFilterByType DEFLATE application/x-font-otf
  AddOutputFilterByType DEFLATE application/x-font-opentype
  AddOutputFilterByType DEFLATE application/vnd.ms-fontobject
  AddOutputFilterByType DEFLATE font/ttf
  AddOutputFilterByType DEFLATE font/otf
  AddOutputFilterByType DEFLATE font/opentype
  
  # Исправление для старых браузеров (на всякий случай)
  BrowserMatch ^Mozilla/4 gzip-only-text/html
  BrowserMatch ^Mozilla/4\.0[678] no-gzip
  BrowserMatch \bMSIE !no-gzip !gzip-only-text/html
</IfModule>

# 3. КЕШИРОВАНИЕ БРАУЗЕРА (Самая важная часть для PageSpeed)
# Заставляет браузер запоминать картинки и стили, чтобы не качать их снова
<IfModule mod_expires.c>
  ExpiresActive On

  # По умолчанию (если тип не указан) - кеш на 5 минут
  ExpiresDefault "access plus 5 minutes"

  # Картинки, видео, шрифты - кешируем на 1 год (они меняются редко)
  ExpiresByType image/jpg "access plus 1 week"
  ExpiresByType image/jpeg "access plus 1 week"
  ExpiresByType image/gif "access plus 1 week"
  ExpiresByType image/png "access plus 1 week"
  ExpiresByType image/webp "access plus 1 week"
  ExpiresByType image/svg+xml "access plus 1 week"
  ExpiresByType image/x-icon "access plus 1 week"
  ExpiresByType video/mp4 "access plus 1 week"
  ExpiresByType video/mpeg "access plus 1 week"
  
  # Шрифты
  ExpiresByType application/font-woff "access plus 1 year"
  ExpiresByType application/font-woff2 "access plus 1 year"
  ExpiresByType application/x-font-woff "access plus 1 year"
  ExpiresByType font/woff "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"

  # CSS и JS - кешируем на 1 месяц
  # Если вы обновите код, браузер может не увидеть изменений сразу.
  # Чтобы сбросить кеш при обновлении сайта, добавляйте версию к файлу: style.css?v=2
  ExpiresByType text/css "access plus 1 week"
  ExpiresByType text/javascript "access plus 1 week"
  ExpiresByType application/javascript "access plus 1 week"
  ExpiresByType application/x-javascript "access plus 1 week"

  # HTML - НЕ кешируем (или очень мало), чтобы изменения текста были видны сразу
  ExpiresByType text/html "access plus 0 seconds"
</IfModule>

# 4. ДОПОЛНИТЕЛЬНЫЕ ЗАГОЛОВКИ (Cache-Control)
<IfModule mod_headers.c>
  # Разрешить кеширование для всех статических файлов
  <FilesMatch "\.(ico|pdf|flv|jpg|jpeg|png|gif|webp|js|css|swf|svg|woff|woff2)$">
    Header set Cache-Control "max-age=2592000, public"
  </FilesMatch>
  
  # Запретить кеширование HTML файлов (чтобы цены и телефоны обновлялись сразу)
  <FilesMatch "\.(html|htm|php)$">
    Header set Cache-Control "max-age=0, private, no-store, no-cache, must-revalidate"
  </FilesMatch>
</IfModule>

# 5. ПОДДЕРЖКА ТИПОВ ФАЙЛОВ
# Убедимся, что сервер понимает WebP и шрифты
<IfModule mod_mime.c>
  AddType image/webp .webp
  AddType font/woff2 .woff2
</IfModule>

# --- КОНЕЦ ФАЙЛА .htaccess ---