# Example only. Do NOT use in production.
# Generate a real file with:
#   htpasswd -c /absolute/path/outside/public_html/.htpasswd-adminka admin
#
# Then set AuthUserFile in adminka_objects/.htaccess to that exact absolute path.
admin:$apr1$example$replaceWithRealHash
