{
  "devDependencies": {
    "esbuild": "^0.20.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build-critical.js"
  },
  "dependencies": {
    "lazysizes": "^5.3.2"
  },
  "type": "module"
}